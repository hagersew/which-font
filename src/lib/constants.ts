/** Prefix for all extension-only CSS classes (avoids collisions with host pages). */
export const EXTENSION_PREFIX = 'which-font';

export const HOST_ID = 'which-font-host';
export const ROOT_ID = 'which-font-root';
export const PORTAL_ID = 'which-font-portal';

export const HOST_CLASS = `${EXTENSION_PREFIX}-host`;
export const ROOT_CLASS = `${EXTENSION_PREFIX}-root`;
export const PORTAL_CLASS = `${EXTENSION_PREFIX}-portal`;
export const CLIPBOARD_CLASS = `${EXTENSION_PREFIX}-clipboard-helper`;
export const MAX_CARDS = 5;
export const MAX_HISTORY = 50;
export const CARD_WIDTH = 320;
export const CARD_MAX_HEIGHT = 420;
export const CARD_OFFSET = 12;
